---
title: "List schedule runs"
description: "List execution runs for a schedule"
api: "GET /v1/schedules/{schedule_id}/runs/"
---

<ParamField path="schedule_id" type="integer" required>
  Schedule ID.
</ParamField>

## SDK

<CodeGroup>
```typescript TypeScript
const runs = await client.schedules.runs(id);
```

```python Python
runs = await client.schedules.runs(schedule_id)
```
</CodeGroup>
